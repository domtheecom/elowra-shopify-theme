<div class="error-page">
  <div class="container">
    <div class="error-content">
      <div class="error-illustration">
        <svg width="200" height="200" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1">
          <circle cx="11" cy="11" r="8"/>
          <path d="M21 21l-4.35-4.35"/>
          <path d="M11 8v3"/>
          <path d="M11 16h.01"/>
        </svg>
      </div>
      
      <div class="error-text">
        <h1 class="error-title">Page Not Found</h1>
        <p class="error-description">
          Sorry, we couldn't find the page you're looking for. It may have been moved, deleted, or you entered the wrong URL.
        </p>
        
        <div class="error-actions">
          <a href="{{ routes.root_url }}" class="btn btn-primary">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
              <polyline points="9,22 9,12 15,12 15,22"/>
            </svg>
            Back to Home
          </a>
          <a href="{{ routes.all_products_collection_url }}" class="btn btn-secondary">Shop Products</a>
        </div>
        
        <div class="search-suggestion">
          <p>Or try searching for what you need:</p>
          <form action="{{ routes.search_url }}" method="get" class="search-form">
            <input 
              type="search" 
              name="q" 
              placeholder="Search products..." 
              class="search-input"
              value="{{ search.terms | escape }}">
            <button type="submit" class="search-button">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="11" cy="11" r="8"/>
                <path d="M21 21l-4.35-4.35"/>
              </svg>
            </button>
          </form>
        </div>
      </div>
    </div>
    
    <div class="helpful-links">
      <h2>Popular Pages</h2>
      <div class="links-grid">
        <a href="{{ collections['ritual-kit'].url }}" class="help-link">
          <h3>The Ritual Kit</h3>
          <p>Our complete 3-step skincare system</p>
        </a>
        <a href="/pages/about" class="help-link">
          <h3>About ELOWRA</h3>
          <p>Learn about our founder and mission</p>
        </a>
        <a href="/pages/faq" class="help-link">
          <h3>FAQ</h3>
          <p>Answers to common questions</p>
        </a>
        <a href="/pages/contact" class="help-link">
          <h3>Contact Us</h3>
          <p>Get in touch with our team</p>
        </a>
      </div>
    </div>
  </div>
</div>

<style>
.error-page {
  padding: 80px 0;
  min-height: 70vh;
  display: flex;
  align-items: center;
}

.error-content {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 60px;
  align-items: center;
  margin-bottom: 80px;
}

.error-illustration {
  text-align: center;
}

.error-illustration svg {
  color: var(--sage-300);
}

.error-title {
  font-size: 48px;
  font-weight: 700;
  margin-bottom: 16px;
  color: var(--gray-900);
}

.error-description {
  font-size: 18px;
  color: var(--gray-600);
  line-height: 1.6;
  margin-bottom: 32px;
}

.error-actions {
  display: flex;
  gap: 16px;
  margin-bottom: 32px;
}

.search-suggestion p {
  color: var(--gray-700);
  margin-bottom: 16px;
}

.search-form {
  display: flex;
  max-width: 400px;
}

.search-input {
  flex: 1;
  padding: 12px 16px;
  border: 1px solid var(--gray-300);
  border-radius: 8px 0 0 8px;
  font-size: 16px;
}

.search-button {
  padding: 12px 16px;
  background: var(--sage-500);
  color: white;
  border: none;
  border-radius: 0 8px 8px 0;
  cursor: pointer;
  transition: var(--transition);
}

.search-button:hover {
  background: var(--sage-600);
}

.helpful-links h2 {
  font-size: 32px;
  font-weight: 700;
  margin-bottom: 32px;
  color: var(--gray-900);
  text-align: center;
}

.links-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 24px;
}

.help-link {
  padding: 24px;
  background: var(--gray-50);
  border-radius: 12px;
  text-decoration: none;
  color: inherit;
  transition: var(--transition);
}

.help-link:hover {
  background: var(--sage-50);
  transform: translateY(-2px);
}

.help-link h3 {
  font-size: 18px;
  font-weight: 600;
  margin-bottom: 8px;
  color: var(--gray-900);
}

.help-link p {
  color: var(--gray-600);
  font-size: 14px;
}

@media (max-width: 768px) {
  .error-content {
    grid-template-columns: 1fr;
    gap: 40px;
    text-align: center;
  }
  
  .error-title {
    font-size: 36px;
  }
  
  .error-actions {
    flex-direction: column;
    align-items: center;
  }
  
  .links-grid {
    grid-template-columns: 1fr;
  }
}
</style>